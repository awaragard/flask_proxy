
sync_args: "--process-groups --users mapped"
root_config: "user-sync-config.yml"
fail_on_error: false
allowed_errors:
  #- can't open file

assertions: |-
  re: (unable to process adobe-only users, as their count )\(\d\d\)( is larger than the max_adobe_only_users setting \(5\))

